export enum FormElement {
  input = 'input',
  select = 'select',
  picker = 'picker',
  list = 'list',
  upload = 'upload',
  filter = 'filter',
  chip = 'chip',
  autocomplete = 'autocomplete'
}

export enum ElementType {
  text = 'text',
  number = 'number',
  password = 'password',
  email = 'email',
  date = 'date',
  time = 'time',
  datetimeLocal = 'datetime-local',
  month = 'month',
  week = 'week',
  url = 'url',
  tel = 'tel',
  color = 'color',
  file = 'file',
  range = 'range',
  select = 'select',
}
